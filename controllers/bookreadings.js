angular.module("bookreadings")
	.constant("firebaseURL", "https://bookreadings.firebaseio.com")
	.controller("bookreadingsCtrl", function ($scope, $firebase, $http, $location, firebaseURL) {
		var i = 0;
	});